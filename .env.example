# ============================================
# Cogito Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# ============================================
# REQUIRED - Server Configuration
# ============================================

# JWT Secret Key (REQUIRED)
# Generate a strong random secret key for JWT token signing
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# OPTIONAL - Server Configuration
# ============================================

# Server Port (default: 3001)
PORT=3001

# Node Environment (development or production)
NODE_ENV=development

# Database Path (default: ./data/cogito.db)
# Absolute or relative path to your SQLite database file
DB_PATH=./data/cogito.db

# Base URL for password reset emails (default: http://localhost:PORT)
# Set this to your production domain, e.g., https://cogito.example.com
BASE_URL=http://localhost:3001

# CORS Allowed Origins (comma-separated)
# In production, specify allowed origins for CORS
# Example: ALLOWED_ORIGINS=https://cogito.example.com,https://www.cogito.example.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# OPTIONAL - Email Configuration (SMTP)
# ============================================
# If not configured, password reset emails will be logged to console in development mode
# For production, configure SMTP to send actual emails

# SMTP Host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=

# SMTP Port (default: 587)
SMTP_PORT=587

# SMTP Secure (true for port 465, false for other ports)
SMTP_SECURE=false

# SMTP Username
SMTP_USER=

# SMTP Password
SMTP_PASS=

# SMTP From Address (defaults to SMTP_USER if not set)
SMTP_FROM=

# ============================================
# OPTIONAL - Client Configuration (Vite)
# ============================================
# These variables must be prefixed with VITE_ to be accessible in the browser

# API Base URL (default: /api in production, http://localhost:3001/api in dev)
# Only needed if your API is on a different domain
VITE_API_URL=

# Client-side Encryption Key (optional)
# Used for password vault encryption on the client side
# Note: This is client-side obfuscation only. For production, use server-side encryption.
# Generate a random key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_ENCRYPTION_KEY=
